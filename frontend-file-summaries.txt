{
  "frontend/src/App.tsx": "Main application entry point that sets up TanStack Router with all routes (home, class, subject, MDCAT, chapter, admin, terms, support), wraps everything in DarkModeProvider, and manages the splash screen display logic using sessionStorage.",
  "frontend/src/pages/HomePage.tsx": "Homepage with hero section, search bar, all five class/MDCAT navigation buttons (9th, 10th, 11th, 12th, MDCAT Prep) hardcoded in the classButtons array, and a features grid — 9th and 10th class buttons restored with teal and orange colors respectively.",
  "frontend/src/pages/ClassPage.tsx": "Class page showing four subject buttons (English, Biology, Chemistry, Physics) in a responsive grid — fixed the invalid bottom-of-file ChevronRight import.",
  "frontend/src/pages/SubjectPage.tsx": "Subject page showing chapters filtered by class and subject with search and recently viewed. Uses strict:false for params, no Edit/Delete buttons on chapter list items — chapters are plain clickable buttons only.",
  "frontend/src/pages/ChapterPage.tsx": "Chapter detail page with tabbed Notes/Audio/Quiz/Flashcards interface; fixes the Blob constructor type error by copying audio bytes into a fresh plain `ArrayBuffer` via `new ArrayBuffer(data.byteLength)` + `set()`, guaranteeing no `SharedArrayBuffer` in the type; supports named selection buttons for dual notes links and dual audio slots with object URL lifecycle management.",
  "frontend/src/pages/MDCATPage.tsx": "MDCAT Prep page showing subject navigation buttons and PDF entries (past papers and practice tests) fetched from the canister. Uses already-mapped PdfEntry types directly from useGetAllPdfEntries, passing string IDs to delete mutations.",
  "frontend/src/pages/MDCATSubjectPage.tsx": "MDCAT subject page showing chapters filtered by subject with search and recently viewed. Uses strict:false for params, no Edit/Delete buttons on chapter list items — chapters are plain clickable buttons only.",
  "frontend/src/pages/AdminPage.tsx": "Admin panel with view-state machine (list/addChapter/editChapter/addPdf/editPdf). Uses already-mapped Chapter/PdfEntry types directly from hooks (no double-mapping), passes string IDs to delete mutations, and uses onSuccess/onCancel props for ChapterForm.",
  "frontend/src/pages/TermsPage.tsx": "Terms & Conditions page with five policy sections; the last condition (Copyright) ends with the word \"Sia\" styled to blend into the surrounding paragraph text, which navigates to /admin when clicked. The old invisible dot entry point has been removed.",
  "frontend/src/pages/SupportPage.tsx": "Support Us page with Easypaisa number 03320335528, copy-to-clipboard button with thank-you animation, and step-by-step payment instructions.",
  "frontend/src/components/PdfEntryForm.tsx": "PDF entry creation/edit form that saves to the canister via useAddPdfEntry/useUpdatePdfEntry mutations. Fixed to use string IDs (no BigInt conversion) and entryType as plain string.",
  "frontend/src/main.tsx": "Application entry point that renders the app with QueryClient and InternetIdentityProvider, and registers the PWA service worker.",
  "frontend/src/components/ChapterForm.tsx": "Chapter creation/edit form with dual notes URL inputs (each with optional label), dual audio upload slots via AudioUploadSlot sub-component (using `RefObject<HTMLInputElement | null>` to match React 19 types), progress bars, and full quiz/flashcard management.",
  "frontend/src/components/AudioPlayer.tsx": "Audio player component supporting both HTTP URLs and blob URLs, with play/pause, seek, volume controls, and graceful error UI. External link fallback is shown only for non-blob HTTP URLs.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend canister operations — CRUD for chapters and PDF entries. All hooks now return already-mapped frontend types (Chapter/PdfEntry with string IDs), and all mutation functions accept string IDs (converting to BigInt internally).",
  "frontend/src/utils/storageService.ts": "Stripped-down localStorage utility that only manages device-local user data: recently viewed chapter IDs, mark-as-completed chapter state, and dark mode preference. All chapter/PDF CRUD and favorites functions have been removed.",
  "frontend/src/types/chapter.ts": "Frontend canonical Chapter type extended with notesUrl1, notesLabel1, notesUrl2, notesLabel2, audioLabel1, audioLabel2, audioMimeType2, hasAudioBlob, and hasAudioBlob2 fields alongside existing fields; PdfEntry type unchanged.",
  "frontend/src/utils/chapterMapper.ts": "Maps backend ChapterSnapshot (not the non-existent Chapter export) to frontend Chapter type, now including all seven new dual-notes/audio label fields and hasAudioBlob/hasAudioBlob2 boolean flags derived from the optional ExternalBlob fields.",
  "frontend/src/components/ChapterCard.tsx": "Chapter card button component that displays chapter title, content badges (Notes/Audio/Quiz/Cards), and completed status icon. Favorites feature removed; uses isChapterCompleted from storageService and quizQuestions/flashcards fields from the updated Chapter type.",
  "frontend/src/components/QuizSection.tsx": "Interactive quiz component that compares selected answer index (number) against correctAnswer (number), fixing all type comparison errors. Removed font-heading class references.",
  "frontend/src/utils/hashPassword.ts": "Utility function that SHA-256 hashes a raw password string using the Web Crypto API (TextEncoder + SubtleCrypto), with no trimming or transformation applied before hashing, returning a lowercase hex string.",
  "frontend/src/components/AdminAuthGuard.tsx": "Password protection wrapper that checks sessionStorage for an `adminAuthenticated` flag; if absent, renders a centered login form that passes the raw password directly to hashPassword (no trimming) and verifies the resulting hash against the backend canister.",
  "frontend/src/components/Header.tsx": "Header component with logo, desktop/mobile navigation links, and dark mode toggle — the admin icon button and mobile admin link have been removed entirely.",
  "frontend/src/utils/audioUploadService.ts": "Deleted — audio upload now uses FileReader to convert files to base64 data URLs inline in ChapterForm, removing the need for a separate service file."
}